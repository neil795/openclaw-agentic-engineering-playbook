# Playbook: CI/CD

Use for `area:infra` issues touching workflows, build lanes, and deployment automation.

## Rules
- Prefer deterministic, cache-aware CI.
- Enforce governance checks before merge.
- Keep deployment rollback-ready.
