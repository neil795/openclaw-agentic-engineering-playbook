<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>Neil Sinha · IT Portfolio</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	:root {
		--bg: #f8fafc;
		--surface: #ffffff;
		--text: #0f172a;
		--muted: #475569;
		--border: #e2e8f0;
		--accent: #0f172a;
	}

	html,
	body {
		margin: 0;
		width: 100%;
		min-height: 100%;
		background: radial-gradient(circle at 10% 0%, #eef2ff 0%, var(--bg) 28%);
		color: var(--text);
	}

	* { box-sizing: border-box; }

	:focus-visible {
		outline: 3px solid #1d4ed8;
		outline-offset: 2px;
		border-radius: 6px;
	}

	.reveal {
		opacity: 0;
		transform: translateY(8px);
		animation: reveal 500ms ease forwards;
	}
	.reveal.d2 { animation-delay: 120ms; }
	.reveal.d3 { animation-delay: 200ms; }

	@keyframes reveal {
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@media (prefers-reduced-motion: reduce) {
		* { scroll-behavior: auto !important; }
		.reveal { opacity: 1; transform: none; animation: none; }
	}
</style>
